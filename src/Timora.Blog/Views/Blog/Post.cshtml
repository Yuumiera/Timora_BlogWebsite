@model Timora.Blog.Models.Post

<article>
    <h1>@Model.Title</h1>
    <p class="text-muted">@Model.PublishedAt.ToLocalTime().ToString("dd.MM.yyyy HH:mm")</p>
    <div>
        @Html.Raw(Model.Content)
    </div>
</article>


